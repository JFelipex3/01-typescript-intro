// Definir variables con valores que no se modifican en el programa.
const name: string = 'JFelipex3';

// Se puede definir el string con una palabra especifica como 'FULL'
let hpPoints: number | string = 95;


const isAlive: boolean = true;


hpPoints = 'FULL';

console.log({
    name, hpPoints, isAlive
});

export {};